
# local secrets — never commit
.env
.env.local

# e2e runtime artifacts (staged pad copy, addresses, screenshots)
e2e/.run/

# transient verification/experiment helpers + logs (not part of the deliverable)
scripts/_*
