.DS_Store
node_modules/
*.log
.env
.venice_key

# large intermediate art (blue-screen raws, regenerable) — keep only processed sprites
game/art_raw/

# buy bot — never commit secrets or runtime state
bot/.env
bot/node_modules/
bot/state.json

# launchpad (Hardhat) — build output & deps
launchpad/node_modules/
launchpad/artifacts/
launchpad/cache/
launchpad/.env
launchpad/typechain-types/
*.env.bak-*

# pad-v4 audit scratch — exploratory PoCs from adversarial audit runs (evidence, not suite)
pad-v4/.audit-scratch/

# pad-v4 audit prototypes — driven only by the gitignored scratch tests above. Kept out of the audit package
# so no unshipped contract compiles into it. H5ObsHook/H5TwapGateVault are TWAP-redesign drafts (the real fix
# will be implemented properly); JC4Probes/ArrowLauncherSaltBound are probes for claims already recorded in
# AUDIT-ROUND-3-EXTERNAL-RESPONSE.md and the H5 regression test.
pad-v4/contracts/test/H5ObsHook.sol
pad-v4/contracts/test/H5ObsHookMin.sol
pad-v4/contracts/test/H5TwapGateVault.sol
pad-v4/contracts/test/JC4Probes.sol
pad-v4/contracts/test/ArrowLauncherSaltBound.sol
