.DS_Store
node_modules/
*.log
.env
.venice_key

# large intermediate art (blue-screen raws, regenerable) — keep only processed sprites
game/art_raw/

# buy bot — never commit secrets or runtime state
bot/.env
bot/node_modules/
bot/state.json
